# architecture-agent Report

- Repository layout fixed around `app`, `components`, `lib`, `prisma`, `skills`, `subagents`, `types`, and `tests`.
- App Router stays server-first; client code is limited to interactive islands.
- Shared relational core supports both games while preserving game-specific detail entities.
- Route map covers all mandatory MVP flows without placeholder pages.
